const container = "flex items-center justify-center";

const loading =
  "animate-spin rounded-full border-t-transparent border-green-500";

const style = { container, loading };

export default style;
