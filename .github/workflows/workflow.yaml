name: Running lint and tests tests workflow

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]

jobs:
  run-lint-tests:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4

      - name: 🧰 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22.16.0"

      - name: 📥 Install dependencies
        run: npm install

      - name: 🔍 Run Lint
        run: npm run lint

      - name: 🧪 Run Tests
        run: npm run test
